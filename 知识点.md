# 前端

## JavaScript 核心（必考）
原型 & 继承

原型链、__proto__ vs prototype
实现 new、instanceof、Object.create
异步

Event Loop（宏任务/微任务执行顺序，必出题）
Promise 原理、手写 Promise.all/race/allSettled
async/await 原理（Generator + 自动执行器）
闭包 & 作用域

变量提升、暂时性死区
闭包经典题（循环 + setTimeout）

手写题（高频）
防抖/节流、深拷贝、柯里化、flat、
bind/call/apply、发布订阅、LRU缓存


## react
版本,核心特性
React 16,Fiber、Error Boundary、Portals、Fragments
React 17,事件委托改为 root，无新特性（升级过渡版）
React 18,并发模式、自动批处理、useTransition、useDeferredValue、Suspense 增强
React 19,Actions、useActionState、useFormStatus、useOptimistic、use()、ref as prop、RSC 稳定

RSC
Fiber 架构 — 为什么要有 Fiber，时间切片
Diff 算法 — 同层比较、key 的作用
Hooks 原理 — useState 链表结构，为什么不能在条件里用
useEffect vs useLayoutEffect
状态管理 — Redux/Zustand 对比，Redux 中间件原理
性能优化 — memo、useMemo、useCallback、lazy

## CSS
BFC 触发条件与应用
flex / grid 布局（必须熟练）
层叠上下文、z-index 失效原因
CSS 动画性能（transform vs top/left）
响应式方案（rem/vw/媒体查询）

## 浏览器 & 网络
浏览器

渲染流程：DOM → CSSOM → Layout → Paint → Composite
重排（reflow）vs 重绘（repaint）
从输入 URL 到页面展示（经典大题）

网络

HTTP/1.1 vs HTTP/2 vs HTTP/3
HTTPS 握手过程（TLS）
缓存策略：强缓存（Cache-Control）vs 协商缓存（ETag/Last-Modified）
跨域解决方案：CORS、JSONP、proxy

## 性能优化
加载：懒加载、代码分割、Tree Shaking、CDN
渲染：虚拟列表（10 万条数据怎么渲染）
指标：LCP、FID/INP、CLS（Core Web Vitals）
工具：Lighthouse、Performance 面板分析