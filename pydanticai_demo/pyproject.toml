[project]
name = "pydanticai-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic-ai[examples]>=1.107.0",
    "anthropic>=0.40.0",
    "python-dotenv",
    "httpx",
]
